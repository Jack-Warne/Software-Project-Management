#pragma once

class Subtraction {
private:
    float a;
    float b;

public:
    // Constructor
    Subtraction(float inputA, float inputB);

    // Function to perform subtraction and output result
    void calculate();
};
